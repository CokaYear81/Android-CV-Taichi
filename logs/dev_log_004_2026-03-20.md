# 开发日志 004 - week2 基础接入：MediaPipe 依赖与 Pose 模型准备完成

## 日期
- 2026-03-20

## 阶段
- 第 2 周
- MediaPipe Pose 接入准备

## 本次目标
- 为现有 Android 相机工程接入 `MediaPipe Tasks Vision`
- 将 `Pose Landmarker` 模型文件放入项目
- 在代码中完成 `PoseLandmarker` 初始化准备

## 本次完成内容

### 1. 接入 `MediaPipe Tasks Vision` 依赖
- 修改 `gradle/libs.versions.toml`
- 新增：
  - `mediapipeTasksVision`
  - `google-mediapipe-tasks-vision`

- 修改 `app/build.gradle.kts`
- 在 `dependencies` 中新增：
  - `implementation(libs.google.mediapipe.tasks.vision)`

### 2. 准备 Pose 模型文件
- 在项目中创建 `assets` 目录占位说明文件：
  - `app/src/main/assets/README_pose_model.txt`
- 下载并放入官方模型文件：
  - `app/src/main/assets/pose_landmarker_lite.task`

### 3. 在代码中加入 Pose 初始化逻辑
- 修改 `app/src/main/java/com/lenovo/taichivision/MainActivity.kt`
- 新增：
  - `MediaPipe` 相关 import
  - `poseLandmarker` 成员变量
  - `setupPoseLandmarker()` 初始化函数
- 在 `onCreate()` 中调用初始化
- 在 `onDestroy()` 中补充 `poseLandmarker` 释放逻辑

### 4. 工程同步与运行结果
- `Gradle sync` 完成
- 项目成功编译并安装到真机
- 当前手机端仍能正常显示：
  - `Analyzing frame`

## 当前判断
- `MediaPipe` 依赖已被工程识别
- `Pose` 模型文件已放到正确位置
- `PoseLandmarker` 初始化代码已加入工程
- 当前还未完成“相机帧送入 Pose 模型”的主链路改造

## 当前项目状态
- `week1` 已完成并通过验收
- `week2` 已完成基础接入部分：
  - 依赖接入
  - 模型接入
  - 初始化准备
- 下一步应进入：
  - `ImageAnalysis -> PoseLandmarker.detectAsync()` 打通

## 当前关键文件
- `D:\nottingham\research\Android_CV\android_app\gradle\libs.versions.toml`
- `D:\nottingham\research\Android_CV\android_app\app\build.gradle.kts`
- `D:\nottingham\research\Android_CV\android_app\app\src\main\assets\pose_landmarker_lite.task`
- `D:\nottingham\research\Android_CV\android_app\app\src\main\assets\README_pose_model.txt`
- `D:\nottingham\research\Android_CV\android_app\app\src\main\java\com\lenovo\taichivision\MainActivity.kt`

## 遇到的现象与解释
- 初次加入 `MediaPipe` 相关代码后，IDE 出现 `Unresolved reference 'mediapipe'`
- 原因不是代码路径错误，而是依赖尚未完成同步
- 完成 `Gradle sync` 后，工程可重新编译运行

## 已知问题
- 当前界面仍只显示原有的 `Analyzing frame`
- 说明 `ImageAnalysis` 还没有真正调用 `PoseLandmarker`
- 目前还没有看到人体关键点结果

## 下一步计划
- 将分析帧转换为 `MPImage`
- 调用 `poseLandmarker.detectAsync(...)`
- 在结果回调中验证是否收到姿态结果
- 完成 week2 的“姿态识别链路打通”第一轮验证

## C 盘占用提醒
- 当前已知相关缓存：
  - `C:\Users\lenovo\.gradle`
- 用途：
  - Gradle 依赖缓存、构建缓存、wrapper 缓存
- 说明：
  - 本次新增 `MediaPipe` 依赖并执行同步、构建后，该目录占用可能进一步增加
- 建议：
  - 当前阶段先不清理
  - 等 week2 主链路跑稳后，再统一评估是否释放 `C:` 空间
