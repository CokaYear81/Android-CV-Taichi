# 开发日志 010 - 分支 `vivotest` 同步与高版本手机兼容性改动核对

## 日期
- 2026-04-09

## 阶段
- `week3` 继续推进期间
- 分支级兼容性修复核对与同步

## 背景
- 项目当前主线仓库位于：
  - `D:\nottingham\research\Android_CV\android_app`
- 当前主分支为：
  - `main`
- 同伴另外创建并推送了一个远程分支：
  - `vivotest`
- 该分支的目标是：
  - 解决部分高版本手机，尤其是新款 `vivo / iQOO` 设备上“应用无法正常运行”的问题

## 本次目标
- 将本地仓库同步到同伴的 `vivotest` 分支
- 核对该分支相对 `main` 的真实修改内容
- 判断这些修改是否属于：
  - 真正的功能兼容修复
  - 还是本地 IDE / 环境配置变化
- 在完成真机验证后，决定是否合并回主线

## 本次完成内容

### 1. 已同步到 `vivotest` 分支
- 本地仓库已切换到：
  - `vivotest`
- 并已从 GitHub 拉取到该分支最新提交
- 当前本地与远程 `origin/vivotest` 已对齐

### 2. 已确认该分支相对 `main` 超前
- 当前 `vivotest` 相对 `main`：
  - 超前 `3` 个提交

### 3. 已核对提交信息
- 当前 `main..vivotest` 范围内的提交为：
  - `5533495` `Readme update`
  - `0f0161c` `mediapipe version change, fit vivo mobilephone`
  - `877fe88` `v1`

### 4. 已完成分支验证与本地合并
- 在 `vivotest` 分支上完成了真机安装与运行测试
- 当前结论：
  - 软件能够正常启动和运行
- 基于当前验证结果，已将：
  - `vivotest`
  - 通过 `fast-forward` 方式合并回本地 `main`
- 本次合并属于：
  - 本地主线同步
- 是否继续推送到远程 `main`，可在后续再决定

## 当前核对到的改动内容

### 一、与兼容性相关的核心改动
以下文件属于真正可能影响“高版本手机能否运行”的关键改动：

- `app/build.gradle.kts`
- `app/src/main/java/com/lenovo/taichivision/MainActivity.kt`
- `gradle.properties`
- `gradle/libs.versions.toml`

从提交信息和 README 更新内容看，当前分支的核心方向是：
- 调整 / 升级 `MediaPipe Tasks Vision`
- 适配新款 `vivo` 系列设备
- 重点处理 Android 15 及 16KB Page Size 相关的 native 崩溃问题

### 二、文档更新
以下文件被更新：
- `README.md`
- `README_EN.md`

README 中已记录的关键信息包括：
- 当前 `week3` 已完成：
  - 正式关键点格式从 `33` 点切换到 `17` 点
  - 新增顶层字段：
    - `landmark_schema_version = "pose17_v1"`
  - overlay 改为仅显示 `17` 点骨架
  - 导出逻辑改为固定 `17` 点，并保留无 pose 帧补零
- 当前 `week4` 已完成：
  - 核心依赖升级：
    - `MediaPipe Tasks Vision -> 0.10.14`
  - 目标是解决：
    - Android 15 / 16KB Page Size 环境下的底层 `SIGSEGV` 崩溃问题
  - 同时提升：
    - 新款 `vivo / iQOO` 设备上的运行稳定性

### 三、本地开发环境相关改动
以下文件也出现在分支差异中，但它们更多属于本地环境或 IDE 配置，不应优先视为核心业务修复：

- `.idea/deploymentTargetSelector.xml`
- `.idea/markdown.xml`
- `.idea/misc.xml`
- `.idea/vcs.xml`
- `.vscode/settings.json`

这些改动应与真正的兼容性修复分开看待。

## 当前验证状态

### 1. 分支同步状态
- 本地 `vivotest` 已成功切换并同步到远程最新版本

### 2. 本地运行状态
- 当前分支版本已在本地手机上重新安装
- 本地确认：
  - App 可以启动
  - 软件测试完成，能够正常运行

### 3. 当前判断
- 该分支不是“坏分支”
- 至少已经证明：
  - 新版本不会导致本地安装 / 启动链路整体失效
- 结合分支命名、提交说明和 README 内容，当前可以合理判断：
  - 该分支的核心价值在于处理高版本手机兼容问题

## 当前需要注意的点
- 目前虽然已经确认 `vivotest` 分支能在本地正常启动，但仍需继续确认：
  - 骨架 overlay 是否正常
  - `Start Capture / Stop Capture` 是否正常
  - `json + mp4` 导出是否正常
- 只有在这些功能都无回归时，才适合进一步考虑：
  - 是否将 `vivotest` 合并回 `main`

## 当前结论
- `vivotest` 分支已经具备明确的技术目的：
  - 提升高版本手机兼容性
- 该分支包含：
  - 依赖与构建配置改动
  - README 进度更新
  - 一部分 IDE / 本地环境配置文件
- 后续评估时应重点区分：
  - 哪些改动该保留进主线
  - 哪些 `.idea / .vscode` 文件不应直接带回主分支
- 当前阶段结论更新为：
  - `vivotest` 已经完成真机验证
  - 软件可正常运行
  - 该分支改动已合并回本地 `main`

## 下一步建议
- 如需继续整理主线，可进一步检查：
  - `.idea / .vscode` 是否要从主线中清理
- 如需与 GitHub 主分支保持一致，可在确认无额外整理项后再执行推送
